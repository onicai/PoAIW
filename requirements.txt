# icpp-pro 6.0.0 switched the smoketest framework from dfx to icp-cli: it no longer
# falls back to the machine-wide active identity and shells out to `icp` instead of
# `dfx`, so the suite fails with "ERROR: no test identity configured".
# This repo still builds and tests with dfx, so cap below 6.0.0.
# Lift the cap as part of the dfx -> icp-cli migration.
icpp-pro>=5.3.1,<6.0.0
bitcoin-utils>=0.6.8
